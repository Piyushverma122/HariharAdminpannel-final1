# Database Configuration
# Edit this file with your MySQL credentials

[DATABASE]
host = localhost
user = root
password = pass123
database = harihar
port = 3306

# Instructions:
# 1. Set your MySQL root password above (if you have one)
# 2. If you use a different MySQL user, change the 'user' field
# 3. If your MySQL runs on a different port, change the 'port' field
# 4. Save this file after making changes
